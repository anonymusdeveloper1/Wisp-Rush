# Godot 4 engine cache (regenerated on import) — never commit
.godot/
/android/

# Builds / exports
/build/
/export/

# Validator & runtime logs (tools/validate.sh)
/logs/
*.log

# Node tooling
node_modules/

# Devlog videos: footage/voice workspace and the local Kokoro TTS environment (large, machine-local)
/video/
/tools/video/.venv/
/tools/video/models/

# Claude Code: personal, machine-local overrides only
.claude/settings.local.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.tmp
