# Legacy art v1 (violet/plum design) — archived 2026-09-11

The pre-redesign `assets/art` tree (PNGs with their original `.import` files), kept for rollback. The `.gdignore` here makes Godot skip this folder, so nothing in the project can reference it.
To restore: move `assets/legacy_v1/art` back to `assets/art`, then delete `assets/legacy_v1/.gdignore`.
