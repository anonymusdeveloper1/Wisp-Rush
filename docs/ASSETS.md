# Asset Registry

> Rules for organising assets plus a registry of every asset pack/file in `assets/` — where it came
> from, its license and how it must be imported. **Every asset that enters the repo gets a row.**
>
> **Status: ✅ redesign v1 art in all runtime paths** ([ADR-0005](decisions/0005-visual-redesign-v1.md)).
> Runtime art under `assets/art/` is **generated** from `concept_art/wisp_rush_redesign_v1/` by
> `tools/art/extract_redesign.py` (+ `tools/art/redesign_v1_slices.json`, see `tools/art/README.md`):
> change the pipeline or the slice spec and re-run it — never hand-edit generated PNGs. The owner
> asset pack v2 is archived in `assets/legacy_v1/` (`.gdignore`, never imported or shipped).
> Release distribution remains blocked on the licence confirmation in [GDD.md](GDD.md) §14.

## Layout

```
assets/
├── art/
│   ├── characters/    player, enemies, NPCs (sprites / sheets / models)
│   ├── environment/   tiles, tilesets, backgrounds, props
│   ├── ui/            icons, panels, buttons, cursors
│   └── vfx/           particle textures, flashes, trails
├── audio/
│   ├── music/         looping tracks (.ogg)
│   └── sfx/           short effects (.wav, or .ogg if large)
├── fonts/             .ttf / .otf (+ license file)
└── shaders/           shared .gdshader files (feature-specific shaders live with their scene)
```

Sub-folders are created as needed; don't pre-create empty ones.

## Naming

- `snake_case`, descriptive, no spaces: `wisp_idle_sheet.png`, `ember_imp_run_sheet.png`.
- Variants get a numeric suffix: `sfx_dash_01.wav`, `sfx_dash_02.wav`.
- Prefix audio by kind: `music_<name>.ogg`, `sfx_<name>.wav`, `ui_<name>.wav`.
- Sprite sheets end in `_sheet`; record frame size in the registry notes (e.g. `32×32, 8 frames`).

## Import rules

- **Never hand-edit `*.import` files.** Change import settings in the editor (Import dock →
  Reimport) or via MCP, then commit the updated `.import` file.
- Pixel art (if the GDD chooses it): set Project Settings → Rendering → Textures → Canvas
  Textures → Default Texture Filter = **Nearest**, and disable mipmaps.
- Music `.ogg` → loop enabled in import settings. SFX `.wav` → no loop.
- Keep original source files (`.aseprite`, `.psd`, `.blend`) outside `assets/` or in a
  `.gdignore`'d `assets/_source/` folder so Godot doesn't import them.

## Intake procedure (when new assets arrive)

1. Inspect the pack: list files, formats, dimensions/frame sizes, included license/readme.
2. Sort into the layout above, renaming to the naming rules (keep a mapping in the registry notes
   if the originals had meaningful names).
3. Add a registry row per pack or file.
4. Run `tools/validate.sh` (imports everything, refreshes `docs/generated/PROJECT_MAP.md`).
5. Add a DEVLOG entry.

### Commissioning an animation loop as whole frames

A loop built from complete drawings only reads as motion if the drawings register. Learned the
expensive way on Ilyra's four storefront frames, which had to be cut back to one held painting.
Anyone ordering frames — from Codex or anywhere else — must ask for all of this, and the intake
must check it before the frames reach a rig:

- **One canvas size for the whole set**, and the same one for every frame.
- **One anchor pixel.** Nominate a body landmark (the hips) and require it at the same canvas
  coordinate in every frame. This is the property that matters most; drift here is what a player
  reads as jitter, and no amount of runtime offset-fitting fully hides it.
- **Small deltas between neighbours.** Consecutive frames differ by a breath, not a new pose. Two
  frames that are separately good drawings of the same character still cut when swapped.
- **At least 12 frames for a ~2 s loop**, and the loop must close: frame N leads back into frame 0.
- **A numbered sequence** (`name_00.png` …), so the extractor can take it without a mapping table.
- **Straight alpha with the RGB under alpha 0 zeroed**, like every other pack here.

Fewer, better-registered frames beat more loose ones. Four unregistered frames animate worse than
one held painting, which is exactly why she holds one.

## Registry

| Path (file or folder) | Kind | Source / author | License | Import notes | Added |
|---|---|---|---|---|---|
| `res://icon.svg` | project icon | created during setup | project-owned | default | 2026-09-11 |
| `res://assets/art/branding/` | stacked wordmark logo, transparent app-icon master (`config/icon`), opaque store icon, splash master | redesign v1: `16_wordmark_logo` (Home/loading logo), `14_brand_mark` (app icon, store icon and splash on `#111521`); `17_game_emblem_icon` is kept in the concept pack unused (owner reverted the icon 2026-09-15) | generated art; confirm before release | logo 1318×956; boot splash set in project.godot | 2026-09-11 |
| `res://assets/art/environment/` | `wisp_rush_arena_background` (arena floor), `home_background`, `menu_background` | redesign v1 `02`, `03`, `04` | generated art; confirm before release | 941×1672 opaque; cover-scale, never letterbox; Home uses home, other menus use menu | 2026-09-11 |
| `res://assets/art/characters/` | Wisp 12, enemies 12, Reaper 8 (+ `09_gate_arrival`, `10_exposed_core`, `11_corridor_cast`, `12_stagger`), forms 6 | redesign v1 `05_v2`, `06_v2`, `07_v2`, `12` (checkerboard removed) | generated art; confirm before release | legacy canvases kept (362 / 444 / 512) and content normalised to legacy occupancy, so scale, pivots and collision fit hold | 2026-09-11 |
| `res://assets/art/environment/props/` (Rifts additions `10`–`16`) | void portal entrance/exit pairs, obsidian pillar (+ cracked), floor warning marker, crumble edge, rubble, rune tile | Rifts v1 pack `05` | generated art; confirm before release | 362×362, state pairs share one scale so switching state never pops | 2026-09-12 |
| `res://assets/art/characters/enemies/` (Rifts additions `13`–`24`) | Cinder Shade, Warden and Rift Spawn, four frames each (art only; no tuning or behaviour) | Rifts v1 pack `06` | generated art; confirm before release | 362×362, one scale per family | 2026-09-12 |
| `res://assets/art/environment/props/` | soul shard (+ cluster, soul spark), split crystal, spike bloom, blade ring (+ `_warning` / `_active` / `_closed` states), void portal | redesign v1 `08` (checkerboard removed) | generated art; confirm before release | legacy 04/05/10/11/12 have no redesign equivalent (archived only) | 2026-09-11 |
| `res://assets/art/environment/rifts/` | four Rift backdrops: shattered, ember hollow, frozen choir, reaper's court | Rifts v1 pack `01`–`04`, generated by the owner from `concept_art/wisp_rush_rifts_v1/GENERATION_PROMPTS.md` | generated art; confirm before release | **must stay 941×1672** or `ARENA_FLOOR_UV` (ADR-0006) misplaces the playfield; frozen choir floor dimmed 0.52 and saturation rescaled per Rift so the Wisp stays the brightest object | 2026-09-12 |
| `res://assets/art/characters/{the_fracture,cinder_maw}/` | 12-frame boss atlases for the Shattered Rift and Ember Hollow | Rifts v2 pack `08`, `09` | generated art; confirm before release | 444×444 canvas matching the Reaper; wired as `BossData` variants (ADR-0010) | 2026-09-12 |
| `res://assets/art/characters/hollow_choir/` | 12-frame second-boss atlas | Rifts v1 pack `07` | generated art; confirm before release | 444×444 canvas matching the Reaper; wired as a `BossData` variant | 2026-09-12 |
| `res://assets/art/environment/endless/<skin_id>.png` (30) and `thumbnails/<skin_id>.png` (30) | Endless arena backgrounds on the shared floor template, and 282×502 Shop card thumbnails | `python3 tools/art/extract_endless.py` from `concept_art/wisp_rush_endless_v1/assets` (checked by `check_endless_skin.py`) | generated art; licence to confirm before release (GDD §14 #1, #21) | 941×1672 opaque RGB; lossy import (quality 0.8, `set_endless_import_lossy.py`); referenced by `data/endless/skins/<skin_id>.tres` (`background_path`, `thumbnail`); never hand-edit | 2026-09-15 |
| `res://assets/art/environment/endless/masks/<skin_id>.png` (10) | Scenery masks of the Legendary/Mythic Endless skins: R emissive light sources, G distortion regions, B scenery weight (0 on the floor, 1 past the 48 px rim tolerance), A zone id | `python3 tools/art/endless_scenery.py` from the runtime backgrounds, the floor template and its `SCENERY` table | derived from the generated backgrounds; same licence note | 471×836 RGBA; lossy import q0.8 (`set_endless_import_lossy.py`), 1.1 MB source → 0.31 MB imported; referenced by `data/endless/skins/<skin_id>.tres` (`scenery.mask`); never hand-edit | 2026-09-16 |
| `concept_art/wisp_rush_endless_v1/assets/` | 30 final-source cosmetic Endless arena backgrounds and `skins_manifest.json` | Codex built-in ImageGen from `SKIN_SET_PROMPT.md` using `floor_template_layout.png` and the owner's playfield guide | generated art; confirm before release | 941×1672 opaque RGB PNG; source pack under `concept_art/.gdignore`, runtime-integrated 2026-09-15 through spec 05's checker/extraction (all 30); keep all 30 on the shared octagonal floor template | 2026-09-15 |
| `res://assets/art/characters/playable/<id>/` (veyra 12, rook 13, morrow 12, bram 17, ilyra 67, noxen 16 + `manifest.json`) | Layers of the six production playable characters plus each one's `preview.png` portrait (HUD, Home and card art) | `python3 tools/art/extract_playable_characters.py` — fixed cells from a rig source for veyra/rook/morrow/bram (`SOURCE_SHEETS`), or a verbatim copy of a per-file part pack for ilyra/noxen (`PART_PACKS`) | generated art; licence to confirm before release (GDD §14 #1, #21) | transparent RGBA, sizes as the cell crops; lossless import (no mipmaps, like the forms); referenced by the rig scenes in `scenes/player/visuals/`; never hand-edit — re-run the extractor and rig builder | 2026-09-17 |
| `concept_art/noxen_v1/` | Noxen's approved blue/cyan neutral assembly, transparent twelve-part sheet, extraction script, per-part rig source and pivot manifest | Codex built-in OpenAI image generation from the owner's approved blue Pinterest-derived direction, then deterministic fixed-region extraction by `extract_parts.py` | generated/derived art; licence to confirm before release | source-only under `concept_art/.gdignore`; regenerate `parts/noxen/`, then run the playable-character extractor and rig builder. Edge-connected neighbour fragments are removed while detached flame sparks are retained | 2026-09-20 |
| `res://assets/art/characters/playable/void/` (2 sheets + portrait) | **Void**, the default Wisp, rebuilt as a whole-frame character. `void_run.png` 3200×1600 (28 frames), `_menu.png` 2784×928 (12), `_portrait.png` 384². Cut to the five animations of GDD §14 #36 on 2026-09-20; the reaction sheet is gone | `python3 tools/art/extract_playable_characters.py void` (`SHEET_PACKS`) | generated art; licence to confirm before release | Same 384 / 512 cells and 8 px gutter as Shade. Loops play at the **authored** rate (`loop_fps_scale` 1.0), not Shade's 1.4 phone compromise. **Its source frames are clipped**: 21 of the 108 draw the character larger than the 512 canvas, so the crystal above its head is severed by the border; the intake removes the severed fragments (`strip_clipped`, ~90k px across 14 states) because the pixels cannot be recovered and a missing crystal reads better than a guillotined one. Worth fixing in the pack | 2026-09-20 |
| `res://assets/art/characters/playable/eclipse/` (2 sheets + portrait) | **Eclipse** rebuilt as a whole-frame character. `eclipse_run.png` 3200×1600 (28 frames), `_menu.png` 2784×928 (12), `_portrait.png` 384². Cut to the five animations of GDD §14 #36 on 2026-09-20; the reaction sheet is gone | `python3 tools/art/extract_playable_characters.py eclipse` (`SHEET_PACKS`) | generated art; licence to confirm before release | The cleanest of the three packs: registered to 0.5 px and **not one frame reaches a border**, so neither intake cleanup is enabled. Its orbiting moon beads are detached by design and `strip_clipped` would have eaten them, which is why that flag is per pack | 2026-09-20 |
| `concept_art/eclipse_wisp_sprite_v1/` | Eclipse whole-frame source pack: 78 gameplay/wall frames at 512², 30 storefront at 724², the review sheet, its JSON row map and `build_sprite_pack.py` | Codex | generated art; licence to confirm before release | source-only and `.gdignore`d; 108 frames, RGB zeroed under alpha 0, no canvas overrun | 2026-09-20 |
| `concept_art/void_wisp_sprite_v1/` | Void whole-frame source pack: 78 gameplay/wall frames at 512², 30 storefront at 724², the 256 px review sheet, its JSON row map and `build_sprite_pack.py` | Codex | generated art; licence to confirm before release | source-only and `.gdignore`d; 108 frames registered to 0.5 px, RGB zeroed under alpha 0. **Known defect:** the character overruns the 512 canvas in the dash, move, attack, wall and storefront frames | 2026-09-20 |
| `concept_art/verdant_shade_sprite_v1/` | Verdant Shade whole-frame review pack: transparent state atlas plus four animation-phase atlases, 78 numbered gameplay/wall frames, 30 numbered storefront frames, exact-row sprite sheet, labelled preview and JSON row map | Codex built-in OpenAI image generation using `concept_art/verdant_shade_v1/concept_board_v2.png` as the identity reference; separately drawn phases change flames, leaf overlap, eyes and root ribbons; `build_sprite_pack.py` preserves registration, adds crisp micro-timing within phases and packs them without cross-fades | generated/derived art; licence to confirm before release | source-only under `concept_art/.gdignore`; 512² gameplay/wall canvases, 724² menu canvases, clean straight alpha; `character_selected` maps to `storefront_flourish`, `character_unlocked` to `storefront_unlock`, `wall_right` mirrors `wall_left`, and `aim_charge` is absent; not integrated | 2026-09-20 |
| `concept_art/ilyra_storefront_video_v1/` | Ilyra storefront-idle video brief: `VIDEO_PROMPT.md` (settings, full / short / negative prompts, reject checklist), `ilyra_reference_green.png` / `_transparent.png` (1024²) and `ilyra_front_clean.png` (607 × 634 native + 24 px margin) | Claude Code, from the approved `ilyra_2_sprite_test` pack | derived from generated art; licence to confirm before release | source-only and `.gdignore`d. **Keyed on green `#00FF00`**, not magenta: 0.0 % of her solid pixels are greenish against 5.3 % magenta-ish and 27.8 % blue-ish. The game's `idle_hover.png` cuts her right boot at the sheet's 627 px quarter line; the clean image restores the 14 missing rows from `pose_sheets/01_core_states.png` without repainting. `ilyra_ready_*` is the approved ready stance (`aim_charge.png`); take 2 of it (`source/ilyra_ready_take2.mp4`, Lumina, 640², 97 frames) is in the game and is the provenance record — it carries the generator's "AI" label, which the intake removes (release check as for Shade) | 2026-09-21 |
| `concept_art/verdant_shade_storefront_video_v1/` | Shade storefront-idle video brief: `VIDEO_PROMPT.md` (tool settings, full / short / negative prompts, reject checklist) and `shade_reference_magenta.png` / `_transparent.png`, 1024² references cut from the pack's `idle_hover_00.png` | Claude Code, from the approved Verdant Shade sprite pack | derived from generated art; licence to confirm before release | source-only and `.gdignore`d. Magenta `#FF00FF` because image-to-video models export no alpha and every other key colour collides with Shade; the generated MP4s go in `source/`. Take 1 (`source/shade_storefront_take1.mp4`, 640², 24 fps, 97 frames, generated with Lumina) is the untouched original and the provenance record: it carries the generator's "AI" content label top left, which the intake removes from the game's copy — check the tool's terms and the store's AI-content disclosure before release | 2026-09-21 |
| `concept_art/void_wisp_sprite_v1/` | Void Wisp whole-frame review pack: identity atlas, four animation-phase atlases, 78 numbered gameplay/wall frames, 30 numbered storefront frames, exact-row transparent sheet, labelled preview, loop GIFs and JSON timing/QA map | Codex built-in OpenAI image generation using the current `assets/art/characters/forms/01_void.png` as the identity reference; separately drawn phases preserve the cyan soul flame, face core, star, crescent plume and diamond while moving flame contours, glow, eyes and ornaments in small steps | generated/derived art; licence to confirm before release | source-only under `concept_art/.gdignore`; 512² gameplay/wall canvases, 724² menu canvases, 108 unique frames, straight alpha with RGB zeroed, content-box centre offset ≤0.5 px; no cross-fades; `wall_right` mirrors `wall_left`, `dash_loop_02` is contact, and `aim_charge` is absent; not integrated | 2026-09-20 |
| `concept_art/eclipse_wisp_sprite_v1/` | Eclipse Wisp redesign and whole-frame review pack: new celestial-corona identity atlas, four animation-phase atlases, 78 numbered gameplay/wall frames, 30 numbered storefront frames, exact-row transparent sheet, labelled preview, loop GIFs and JSON timing/QA map | Codex built-in OpenAI image generation using the current `assets/art/characters/forms/06_eclipse.png` as the material reference; the redesigned handless Wisp uses an obsidian face-core, opposing violet/gold broken-eclipse crescents, three lower flame ribbons and orbiting moon beads | generated/derived art; licence to confirm before release | source-only under `concept_art/.gdignore`; 512² gameplay/wall canvases, 724² menu canvases, 108 unique frames, straight alpha with RGB zeroed, content-box centre offset ≤0.5 px; clipped atlas-edge fragments removed deterministically without touching legitimate beads; `wall_right` mirrors `wall_left`, `dash_loop_02` is contact, and `aim_charge` is absent; not integrated | 2026-09-20 |
| `res://assets/art/characters/playable/noxen/` (12 rig layers, 3 VFX, preview + manifest) | Noxen, the Veilflame: handless cloak creature with two skinned root ribbons and separate fins, hood, mask, core, shadow tail and flame horns | `python3 tools/art/extract_playable_characters.py noxen` (`PART_PACKS`), from `concept_art/noxen_v1/parts/noxen/` | generated art; licence to confirm before release (GDD §14 #1, #21) | transparent RGBA, copied verbatim so manifest pivots remain valid; `preview.png` is cropped from the transparent assembly; never hand-edit | 2026-09-20 |
| `concept_art/wisp_rush_playable_characters_v1/` | `references/<id>_concept.jpg` (the owner's three character sheets) and `assets/<id>_rig_source.png` (isolated 4×3 layer sheets) | owner-supplied concepts; sheets generated by Codex with the OpenAI image tool from them (`GENERATION_PROMPT.md`) | generated from owner-supplied references; licence to confirm | 1448×1086 RGBA sheets under `concept_art/.gdignore`; never a runtime texture — only the extracted layers ship | 2026-09-17 |
| `concept_art/wisp_rush_playable_characters_v2/` | owner-approved reference sheets for Mythic Ilyra and Legendary Bram, exact prompts and the sequential Claude implementation handoff | Codex built-in OpenAI image generation from `GENERATION_PROMPTS.md`; owner concepts and approved Bram used only as rendering references | generated art; licence to confirm before release | RGB/RGBA concept PNGs under `concept_art/.gdignore`; never runtime textures — create isolated transparent rig sources/layers per `IMPLEMENTATION_BRIEF.md` | 2026-09-18 |
| `concept_art/wisp_rush_playable_characters_v3/` | Ilyra's v3.1 skinned rig source: 55 transparent part PNGs plus `parts/ilyra/manifest.json` (pivot, tip, rest transform, draw order and parent per part; four-point deformation chains on torso, four whole arms and two whole legs), an assembly reference, `ART_REQUEST.md` and its `ART_REQUEST_SKINNED.md` supersession | Codex built-in OpenAI image generation against the approved v2 identity lock; the continuous torso losslessly flattens the approved v3 chest/hips and the other 48 retained parts are byte-identical | generated/derived art; licence to confirm before release | under `concept_art/.gdignore`; never runtime textures. Claude must update the extractor and rig builder for the 55-part skinned contract; per-file pivots are in local pixel space, so never re-crop a part independently | 2026-09-18 |
| `concept_art/wisp_rush_playable_characters_v2/assets/` | `ilyra_rig_source.png` and `bram_rig_source.png`: transparent rig sources derived from the approved reference sheets | `python3 tools/art/make_rig_source.py` (Ilyra keeps the reference's own subject matte; Bram is keyed off near-black) | derived from generated art; licence as the references | 1536×1024 RGBA under `concept_art/.gdignore`; never a runtime texture and never hand-edited — re-run the tool, then the extractor | 2026-09-18 |
| `concept_art/wisp_rush_playable_characters_v3/` | Claude-authored rig-art contracts, Ilyra v3.1 source-pack README and verbatim generation/finishing provenance; Bram remains gated | Codex following `ART_REQUEST.md` plus the superseding `ART_REQUEST_SKINNED.md`, using built-in OpenAI image generation and deterministic approved-pixel preservation | generated/derived art; licence to confirm before release | source-only and `.gdignore`d; Ilyra skinned replacement complete, no Bram parts until the owner finishes the Ilyra phone gate | 2026-09-18 |
| `concept_art/wisp_rush_playable_characters_v3/references/` | unchanged identity-lock copies of the approved Ilyra and Bram concept sheets | byte-preserving copies from `wisp_rush_playable_characters_v2/references/` | same generated-art licence as v2; confirm before release | comparison authority only; never display or ship a complete sheet | 2026-09-18 |
| `concept_art/wisp_rush_playable_characters_v3/parts/ilyra/` | 55 one-part RGBA rig sources plus `manifest.json`: expressions, one continuous torso, two shoulder ornaments, four continuous four-point-chain arms, bare hands, folding fan, two continuous four-point-chain legs plus boots, cloth/crown/core and seven VFX textures | Codex built-in OpenAI image generation from the exact prompts in the v3 pack; torso and the other 48 retained components preserve approved pixels | generated/derived art; licence to confirm before release | straight alpha, ≥8 px transparent padding, 1024 px manifest coordinate frame; source-only, never load these directly until Claude's extractor/skinned-rig rebuild | 2026-09-18 |
| `concept_art/wisp_rush_playable_characters_v3/assembly/` | `ilyra_assembly_reference.png`, the neutral 1024² front-view registration target for the v3.1 skinned cut | deterministically composited from `parts/ilyra/manifest.json`; no post-painting | derived from the v3.1 Ilyra sources; same licence note | QA reference only, not a sprite; four arms and two legs have no old rigid-segment seams, alternate expressions/hands/lit membrane/VFX are hidden and the shared fan set is instanced twice | 2026-09-18 |
| `res://assets/art/characters/playable/ilyra/` (20 poses) | One finished painting per animation state and per arena surface for Ilyra 2, the whole-frame character every new one now follows (GDD §14 #34) | `python3 tools/art/extract_playable_characters.py ilyra` — the `POSE_PACKS` intake copies the reviewed `concept_art/ilyra_2_sprite_test/final_sprites/` PNGs **byte for byte** and measures each one's silhouette into `data/characters/ilyra_poses.tres` | generated art; licence to confirm before release (GDD §14 #1, #21) | transparent RGBA, all 627×627; lossless import (no mipmaps, like the forms); `idle_hover.png` doubles as the HUD and card portrait; the trail and dash particles use her own `ornaments/sparkle_small.png` and `ornaments/slash_arc.png`; never hand-edit — re-run the extractor | 2026-09-19 |
| `res://assets/art/characters/playable/ilyra/ornaments/` (8) | Ilyra's loose pieces — two sparkle sizes, a sparkle pair, three crown shards, her chest gem glow and the slash arc — drawn drifting around her by `CharacterAura` rather than painted into a pose | `python3 tools/art/extract_playable_characters.py ilyra` (`ORNAMENT_PACKS`), from `concept_art/ilyra_2_sprite_test/detached_parts/sprites/secondary_motion/` | generated art; licence to confirm before release | RGB under alpha 0 is zeroed on the way in so Godot's `fix_alpha_border` has nothing stale to bleed; the kit ships `dash_slash.png` and `sparkle_pair.png` under **swapped** names and the intake corrects them. Six of the eight are in her drifting ring; `slash_arc.png` is her dash-particle texture and `sparkle_small.png` her trail; `chest_gem_glow.png` is extracted and **not yet used** | 2026-09-19 |
| `res://assets/art/characters/playable/ilyra/storefront/` (4) | Her menu body. Only two of the four are used: Her menus play `ilyra_menu.ogv` (ADR-0016); `storefront_welcome` is held only under Reduced Motion, and `storefront_flourish` is no longer shown (no pick/purchase bounce since 2026-09-21). `storefront_blink` and `storefront_settle` are extracted and unused — cycling four whole paintings changed her entire silhouette each step and read as a flip-book | `python3 tools/art/extract_playable_characters.py ilyra` (`FRAME_PACKS`), copied verbatim | generated art; licence to confirm before release | 600×724 each, **not** the 627 gameplay canvas — the equal canvas is what keeps her pivot stable, and the rig draws them at 627/724 so she is the same height as in a run | 2026-09-19 |
| `res://assets/art/characters/playable/ilyra/puppet/` (26) | **Experimental sprite test, currently unused by the game.** Her figure cut into head, torso, waist, skirt, legs, boots, braids, four arm chains and two fans. Extracted for a jointed menu puppet that was built and then reverted (DEVLOG 2026-09-20); kept because the intake is deterministic and the parts are the material for any future part-level motion | `python3 tools/art/extract_playable_characters.py ilyra` (`PUPPET_PACKS`), from `concept_art/ilyra_2_sprite_test/detached_parts/sprites/` | generated art; licence to confirm before release | each limb carries gold caps at its joints, so shoulder/elbow/wrist are findable by colour rather than by eye. **Gap:** `hand_grip_a` is the only closed fist — `hand_grip_b`, `hand_relaxed_a` and `hand_relaxed_b` are all open hands | 2026-09-20 |
| `res://assets/art/characters/playable/ilyra/ilyra_menu.ogv` | Ilyra's menu video: her ready stance, a 72-frame, 3.00 s, 24 fps loop, packed colour \| matte at 1280 × 640, Ogg Theora | `GODOT_PATH=… python tools/art/extract_menu_video.py ilyra` from `concept_art/ilyra_storefront_video_v1/source/ilyra_ready_take2.mp4` | generated art; licence to confirm before release | ~2.0 MB. **Green key.** Loop cut at frame 72, seam 3.36 against a 3.28 median step; matte floor 0.071, ceiling 0.937. ADR-0016 | 2026-09-21 |
| `res://assets/art/characters/playable/verdant_shade/verdant_shade_menu.ogv` | Shade's menu video: an 80-frame, 3.33 s, 24 fps loop, packed colour \| matte at 1280 × 640, Ogg Theora (+ a silent Vorbis track Movie Maker always writes) | `GODOT_PATH=… python tools/art/extract_menu_video.py verdant_shade` from `concept_art/verdant_shade_storefront_video_v1/source/` | generated art; licence to confirm before release | ~1.2 MB. Encoded by Godot's Movie Maker, not ffmpeg (Windows ffmpeg writes broken Theora); the tool verifies frame count and rate. ADR-0016 | 2026-09-21 |
| `res://assets/art/characters/playable/verdant_shade/` (2 sheets + portrait) | **Verdant Shade**, the first character built to the whole-frame contract. `verdant_shade_run.png` 3200×1600 (28 frames), `_menu.png` 2784×928 (12), `_portrait.png` 384². Cut to the five animations of GDD §14 #36 on 2026-09-20; the reaction sheet is gone | `python3 tools/art/extract_playable_characters.py verdant_shade` (`SHEET_PACKS`) packs the approved 512² gameplay and 724² storefront frames onto a fixed grid and generates `data/characters/verdant_shade_{gameplay,menu}.tres` | generated art; licence to confirm before release | 384 px gameplay cells and 512 px menu cells — smaller than delivered on purpose, because the character draws at ~230 px in a run and ~520 px on a card. **Every cell carries an 8 px transparent gutter**: without it the canvas filter samples past the atlas region and sticks a sliver of the *next* frame onto the current one, which is exactly what the first build did on the phone. Every sheet stays inside the 4096 px texture floor. Looping animations play at 1.4× their authored rate (`loop_fps_scale`); one-shots are unscaled because their rates match `STATE_LENGTHS`. The anchor is the cell centre by construction; measured residual drift is recorded in `data/characters/verdant_shade_frames.tres`. **Gap:** no separate ornament or VFX textures exist for her yet, so her rig emits no particles and her trail is whatever the frames paint plus the procedural dash signature | 2026-09-20 |
| `concept_art/verdant_shade_sprite_v1/` | Verdant Shade whole-frame source pack: 78 gameplay/wall frames at 512², 30 storefront frames at 724², five phase atlases, the 256 px review sheet and its JSON row map, `build_sprite_pack.py`, README and generation prompt | Codex, from the approved `concept_art/verdant_shade_v1/concept_board_v2.png` | generated art; licence to confirm before release | source-only and `.gdignore`d; 108 frames, straight alpha with RGB zeroed under alpha 0, and a measured content-box centre spread of **0.5 px** across the whole pack — the registration the whole-frame contract asks for | 2026-09-20 |
| `concept_art/ilyra_2_sprite_test/` | Whole-frame Ilyra source pack: 20 reviewed 627×627 transparent state/surface sprites, five source pose sheets, two corrected wall-pose masters, final contact sheet, generation prompts and Claude handoff | Codex built-in OpenAI image generation using the approved Ilyra v3 concept/runtime preview as identity references and the owner-supplied climber sheet as gesture-only wall reference | generated/derived art; licence to confirm before release | source-only concept pack; `final_sprites/` is the review candidate, not runtime art. Promoted to the shipping Ilyra on 2026-09-20; the runtime copy is made by a deterministic extractor into `assets/art/characters/playable/ilyra/` | 2026-09-19 |
| `res://assets/art/vfx/` | 12 legacy effects + `13_victory_pulse` | redesign v1 `09` (black background → straight alpha) | generated art; confirm before release | normal blending is correct; additive would double-brighten | 2026-09-11 |
| `res://assets/art/ui/` | `system/` icons (10 legacy + `13_daily`…`18_reaper`), `mutations/` 8, `frames/` 13 frame-kit pieces | redesign v1 `11`, `13_v2`, `10` | generated art; confirm before release | `09_mute`, `12_store` dropped; frames feed the theme | 2026-09-11 |
| `res://assets/ui/theme/` | project Theme, nine-patch textures, ornaments | built from `assets/art/ui/frames/` by `assets/ui/theme/tools/*` | project-owned build | rebuild steps in [ui_design_system.md](systems/ui_design_system.md); never hand-edit the `.tres` | 2026-09-11 |
| `res://assets/legacy_v1/` | archived owner asset pack v2 (all rows below) | owner-supplied `Wisp Rush Godot Release MVP v2.zip` | not stated | `.gdignore` — restore = move back + delete `.gdignore` | 2026-09-11 |
| `legacy_v1/art/branding/` | logo, 2048² icon master, portrait splash master | owner-supplied `Wisp Rush Godot Release MVP v2.zip` | not stated; confirm before release | PNG; original names normalised from kebab-case | 2026-09-11 |
| `legacy_v1/art/environment/wisp_rush_arena_background.png` | full-bleed arena | same pack, `master-sheets/world/` | not stated; confirm before release | 941×1672 PNG; cover-scale and crop, never letterbox | 2026-09-11 |
| `legacy_v1/art/characters/wisp/` | 12 Wisp animation states | same pack, `individual-sprites/riftling/` | not stated; confirm before release | 362×362 RGBA frames; numeric order retained; runtime name is Wisp | 2026-09-11 |
| `legacy_v1/art/characters/enemies/` | 12 frames across three enemy families | same pack, `individual-sprites/enemies/` | not stated; confirm before release | 362×362 RGBA; Soul Wisp, Shard Wraith, Bone Mote | 2026-09-11 |
| `legacy_v1/art/characters/reaper/` | 8 Reaper boss states | same pack, `individual-sprites/reaper/` | not stated; confirm before release | 444×444 source cells exported as individual PNGs | 2026-09-11 |
| `legacy_v1/art/characters/forms/` | 6 cosmetic forms | same pack, `individual-sprites/forms/` | not stated; confirm before release | 512×512 RGBA; cosmetic visuals only | 2026-09-11 |
| `legacy_v1/art/environment/props/` | 12 pickup/obstacle/prop sprites | same pack, `individual-sprites/props/` | not stated; confirm before release | 362×362 RGBA | 2026-09-11 |
| `legacy_v1/art/vfx/` | 12 combat/teleport/collectible effects | same pack, `individual-sprites/vfx/` | not stated; confirm before release | 362×362 RGBA; pool frequent effects later | 2026-09-11 |
| `legacy_v1/art/ui/system/` | 12 system icons | same pack, `individual-sprites/ui/` | not stated; confirm before release | 362×362 RGBA; keep touch target larger than artwork | 2026-09-11 |
| `legacy_v1/art/ui/mutations/` | 8 mutation icons | same pack, `individual-sprites/mutations/` | not stated; confirm before release | 362×362 RGBA | 2026-09-11 |

## Intake provenance

- Source archive: owner-supplied `Wisp Rush Godot Release MVP v2.zip`, received 2026-09-11.
- Build prompt: standalone and archived copies are byte-identical (SHA-256
  `c174ff790b03ed583efac91ef2b44d46e2f6e80b727890800918a4b6fc0ab60b`).
- Imported runtime set: 86 PNG files (82 individual sprites, three branding masters, one arena).
- Original numeric prefixes are retained; hyphens changed to underscores for project naming rules.
- Master sprite sheets, platform convenience exports, contact sheets and all `reference/legacy-*`
  files remain outside `res://` and therefore cannot enter a game export.
- The pack contains no audio, fonts, license, copyright or author metadata.
